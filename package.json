{
  "name": "temporal",
  "type": "module",
  "private": true,
  "bin": {
    "temporal": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build --compile --minify src/index.ts --outfile temporal",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
