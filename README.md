# temporal

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run src/index.ts --help
```

This project was created using `bun init` in bun v1.3.9. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.

## Reference Material Notice

This repository intentionally does not publish extracted third-party reference content.
The folder `specs/001-time-cli/references/` (and local source PDF files used during research)
is excluded from version control for public distribution.

For the underlying academic source, see the citation in `agents.md` and obtain the work
from official publishers/libraries.
